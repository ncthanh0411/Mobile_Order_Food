<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.food_order_application_2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_bottomNav">
       </activity>
        <activity android:name=".Menu.activity_cart_detail" />
        <activity android:name=".Menu.activity_food_detail_demo" />
        <activity android:name=".Menu.activity_menu" />
        <activity android:name=".Login_activity.activity_Login_signup" />

            <activity
                android:name=".Login_activity.activity_Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>